{
  "version": 2,
  "builds": [
    { "src": "*.js", "use": "@vercel/node" }
  ],
  "routes": [
    {
      "src": "/api/auth/(.*)",  
      "dest": "/api/auth/$1"
    },
    {
      "src": "/receipe/(.*)", 
      "dest": "/receipe/$1"
    }
  ]
}
